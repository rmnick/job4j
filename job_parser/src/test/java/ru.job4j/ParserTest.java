package ru.job4j;

import org.junit.Test;

import java.sql.SQLException;
import java.util.concurrent.ArrayBlockingQueue;
import java.util.concurrent.BlockingQueue;

public class ParserTest {
    @Test
    public void test() throws Throwable {
        /*
        //Config config = new Config();
        //Connection con = DriverManager.getConnection(config.get("url"), config.get("username"), config.get("password"));
        //BlockingQueue<Vacancy> vacancies = new ArrayBlockingQueue<>(50);
        //VacancyLoader vl = new VacancyLoader(con, vacancies);
        //PageParser pp = new PageParser(config, vacancies);
        //pp.createDb();
        //System.out.println(pp.parseDate("1 фев 19, 11:16"));
        //pp.firstParse();
        //pp.parse(pp.parseDate("1 фев 19, 11:16"));
        //pp.start();
        */
    }
}
